<h1>新規投稿</h1>
<%= form_with model: @task, local: true do |f| %>
    <P>タイトル<%= f.text_field :title %></P>
    <p>内容<%= f.text_field :content %></p>
    <%= f.submit "送信" %>
<% end %>
<%= link_to "一覧表示へ", tasks_path %>